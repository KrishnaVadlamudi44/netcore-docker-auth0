import Layout from './Layout';

const Dashboard = () => {
  return (
    <div>
      <div>
        <Layout />
      </div>

      <div>Dashboard</div>
    </div>
  );
};

export default Dashboard;
